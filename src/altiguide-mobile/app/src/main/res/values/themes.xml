<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Altiguidemobile" parent="Theme.AppCompat.Light.NoActionBar" />

    <!-- Splash theme: fullscreen, transparent status bar and matching background -->
    <style name="Theme.Altiguidemobile.Splash" parent="Theme.Altiguidemobile">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowBackground">@drawable/bg_gradient_onboarding</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">#E1FF74</item>
    </style>
</resources>